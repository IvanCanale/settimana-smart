---
phase: 08-legal-privacy-policy-e-terms-of-service-pagine-pubbliche-consenso-esplicito-al-signup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/privacy/page.tsx
  - src/app/terms/page.tsx
autonomous: true
requirements:
  - LEGAL-01
  - LEGAL-02

must_haves:
  truths:
    - "Visiting /privacy renders an Italian-language Privacy Policy page without errors"
    - "Visiting /terms renders an Italian-language Terms of Service page without errors"
    - "Both pages have a back link to the app"
    - "Both pages are accessible without authentication"
  artifacts:
    - path: "src/app/privacy/page.tsx"
      provides: "Privacy Policy public page at /privacy"
      exports: ["default (PrivacyPage)"]
    - path: "src/app/terms/page.tsx"
      provides: "Terms of Service public page at /terms"
      exports: ["default (TermsPage)"]
  key_links:
    - from: "src/app/privacy/page.tsx"
      to: "/"
      via: "← Torna all'app link"
      pattern: "href.*/"
    - from: "src/app/terms/page.tsx"
      to: "/"
      via: "← Torna all'app link"
      pattern: "href.*/"
---

<objective>
Create two public Next.js App Router pages — /privacy and /terms — with Italian-language legal content styled to match the existing app design system.

Purpose: Provide required legal documentation reachable from the consent checkbox in the onboarding signup step (Plan 02) and from direct URL access.
Output: src/app/privacy/page.tsx and src/app/terms/page.tsx, each a static server component with no external dependencies.
</objective>

<execution_context>
@C:/Users/ivan9/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/ivan9/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md

<interfaces>
<!-- Design system tokens available in globals.css. Use inline styles matching OnboardingFlow patterns. -->

CSS variables (from src/app/globals.css):
  --terra: #C4673A
  --terra-light: #E8856A
  --terra-dark: #9B4E2B
  --cream: #FAF5ED
  --cream-dark: #F0E8D8
  --sepia: #3D2B1F
  --sepia-light: #6B4C3B
  --warm-white: #FFFDF8

CSS classes available:
  .bg-texture        — cream background with subtle pattern
  .card-warm         — warm-white card with border-radius:20px and shadow
  .font-display      — Playfair Display serif
  .btn-terra         — terra-colored button

Layout pattern from OnboardingFlow.tsx:
  Outer: className="bg-texture" style={{ minHeight:"100vh", display:"flex", alignItems:"center", justifyContent:"center", padding:20 }}
  Card:  className="card-warm" style={{ maxWidth:480, width:"100%", padding:"clamp(20px, 6vw, 36px)" }}
  For legal pages use maxWidth:680px (wider than onboarding cards, more readable for long text)

Next.js App Router: no "use client" needed — these are static server components.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Privacy Policy page at /privacy</name>
  <files>src/app/privacy/page.tsx</files>
  <read_first>
    - src/app/globals.css (lines 131-200, for CSS class names and variables)
    - src/app/layout.tsx (to confirm AuthProvider wraps all routes — legal pages inherit it but don't need it)
  </read_first>
  <action>
Create src/app/privacy/page.tsx as a Next.js App Router server component (no "use client").

File structure:
```tsx
export default function PrivacyPage() {
  return (
    <div className="bg-texture" style={{ minHeight: "100vh", padding: "20px 20px 60px" }}>
      <div style={{ maxWidth: 680, margin: "0 auto" }}>
        {/* Back link */}
        <a href="/" style={{ display: "inline-flex", alignItems: "center", gap: 6, color: "var(--sepia-light)", fontSize: 14, textDecoration: "none", marginBottom: 32, marginTop: 20 }}>
          ← Torna all'app
        </a>
        <div className="card-warm" style={{ padding: "clamp(24px, 6vw, 48px)" }}>
          {/* Header */}
          <h1 className="font-display" style={{ fontSize: "clamp(24px, 5vw, 36px)", color: "var(--sepia)", marginBottom: 8, marginTop: 0 }}>
            Privacy Policy
          </h1>
          <p style={{ color: "var(--sepia-light)", fontSize: 14, marginBottom: 40, marginTop: 0 }}>
            Ultimo aggiornamento: marzo 2026
          </p>
          {/* Sections rendered as helper function or inline JSX */}
          ...
        </div>
      </div>
    </div>
  );
}
```

Section helper pattern (avoid repetition):
```tsx
function Section({ title, children }: { title: string; children: React.ReactNode }) {
  return (
    <section style={{ marginBottom: 32 }}>
      <h2 className="font-display" style={{ fontSize: 20, color: "var(--sepia)", marginBottom: 12, marginTop: 0 }}>
        {title}
      </h2>
      <div style={{ color: "var(--sepia-light)", fontSize: 15, lineHeight: 1.7 }}>
        {children}
      </div>
    </section>
  );
}
```

Italian content — include ALL of the following sections in order:

**1. Titolare del trattamento**
Settimana Smart è un servizio offerto da un privato. Per qualsiasi questione relativa ai tuoi dati, contattaci a: privacy@settimana-smart.app

**2. Dati raccolti**
Raccogliamo i seguenti dati per fornirti il servizio:
- Preferenze alimentari (dieta, intolleranze/allergie, numero di persone, tempo di cottura)
- Piani pasto settimanali generati dall'app
- Abbonamenti alle notifiche push (endpoint del dispositivo, senza informazioni personali identificabili)
- Indirizzo email e dati di autenticazione, se scegli di creare un account

Nessun dato viene venduto a terze parti.

**3. Come utilizziamo i dati**
I tuoi dati vengono utilizzati esclusivamente per:
- Generare piani pasto personalizzati in base alle tue preferenze
- Sincronizzare i tuoi piani tra dispositivi (se hai un account)
- Inviare notifiche push per i promemoria della spesa (solo se hai dato il consenso)

**4. Fornitore di servizi (sub-responsabile)**
Utilizziamo Supabase (supabase.com) come fornitore di database e autenticazione. I dati sono conservati su server in Europa. Supabase agisce come responsabile del trattamento ai sensi del GDPR.

**5. Conservazione dei dati**
I dati vengono conservati finché il tuo account è attivo. Puoi richiedere la cancellazione in qualsiasi momento tramite la sezione Profilo dell'app (Elimina account). I dati vengono eliminati entro 30 giorni dalla richiesta.

**6. I tuoi diritti**
Hai il diritto di:
- Accedere ai tuoi dati: usa la funzione "Esporta i miei dati" nella sezione Profilo
- Cancellare il tuo account e tutti i dati associati: usa "Elimina account" nella sezione Profilo
- Opporti al trattamento: contattaci a privacy@settimana-smart.app

**7. Cookie e archiviazione locale**
L'app utilizza il localStorage del browser per salvare le preferenze in locale. Non utilizziamo cookie di profilazione o tracker di terze parti.

**8. Contatti**
Per qualsiasi domanda relativa alla privacy: privacy@settimana-smart.app

Use `<p>` tags for paragraph text within sections. Use `<ul><li>` for bullet lists with style={{ paddingLeft: 20, margin: "8px 0" }} on ul and style={{ marginBottom: 6 }} on li. Use `<a href="mailto:privacy@settimana-smart.app">` for the email link with style={{ color: "var(--terra)" }}.

Do NOT add "use client". Do NOT import anything from supabase. No external library imports.
  </action>
  <verify>
    <automated>cd /c/Users/ivan9/Desktop/settimana-smart/.claude/worktrees/zen-austin && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "Privacy Policy" src/app/privacy/page.tsx
    - grep -q "privacy@settimana-smart.app" src/app/privacy/page.tsx
    - grep -q "Torna all'app" src/app/privacy/page.tsx
    - grep -q "Supabase" src/app/privacy/page.tsx
    - grep -q "use client" src/app/privacy/page.tsx returns no match (server component)
  </acceptance_criteria>
  <done>src/app/privacy/page.tsx exists, renders Privacy Policy in Italian with all 8 sections, has a "← Torna all'app" back link to "/", contact email privacy@settimana-smart.app, and TypeScript compiles clean.</done>
</task>

<task type="auto">
  <name>Task 2: Create Terms of Service page at /terms</name>
  <files>src/app/terms/page.tsx</files>
  <read_first>
    - src/app/privacy/page.tsx (just created — mirror its layout and Section helper exactly)
  </read_first>
  <action>
Create src/app/terms/page.tsx following the exact same file structure as privacy/page.tsx (same layout wrapper, same Section helper, same back link, same styling). Only the content differs.

Italian content — include ALL of the following sections in order:

**1. Il Servizio**
Settimana Smart è un'applicazione web per la pianificazione dei pasti settimanali. Il servizio genera piani pasto personalizzati, liste della spesa e ricette basate sulle preferenze dell'utente.

**2. Utilizzo del Servizio**
Utilizzando Settimana Smart, accetti di:
- Usare il servizio solo per scopi personali e non commerciali
- Non tentare di accedere ai sistemi interni o al database
- Non usare il servizio per generare contenuti illegali o dannosi

**3. Account**
La creazione di un account è opzionale. Se scegli di registrarti, sei responsabile della riservatezza delle tue credenziali. Non condividere le tue credenziali di accesso con terzi.

**4. Contenuti generati dall'IA**
Le ricette e i piani pasto sono generati da algoritmi e intelligenza artificiale. Settimana Smart non garantisce l'accuratezza nutrizionale delle informazioni fornite. Per esigenze mediche specifiche (allergie gravi, patologie), consulta sempre un professionista della salute.

**5. Limitazione di Responsabilità**
Il servizio è fornito "così com'è", senza garanzie di alcun tipo. Settimana Smart non è responsabile per:
- Interruzioni del servizio o perdita di dati
- Decisioni prese sulla base dei piani pasto generati
- Errori nelle informazioni sugli ingredienti o nelle ricette

**6. Modifiche al Servizio**
Ci riserviamo il diritto di modificare o interrompere il servizio in qualsiasi momento, con o senza preavviso.

**7. Legge Applicabile**
I presenti Termini sono regolati dalla legge italiana. Per qualsiasi controversia, è competente il Foro di residenza dell'utente.

**8. Contatti**
Per qualsiasi domanda: privacy@settimana-smart.app

Page title: "Termini di Servizio" (h1), subtitle: "Ultimo aggiornamento: marzo 2026"

Same structural rules as privacy page: server component, no "use client", no external imports, inline styles matching design system, email as mailto link with terra color.
  </action>
  <verify>
    <automated>cd /c/Users/ivan9/Desktop/settimana-smart/.claude/worktrees/zen-austin && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "Termini di Servizio" src/app/terms/page.tsx
    - grep -q "privacy@settimana-smart.app" src/app/terms/page.tsx
    - grep -q "Torna all'app" src/app/terms/page.tsx
    - grep -q "legge italiana" src/app/terms/page.tsx
    - grep -q "use client" src/app/terms/page.tsx returns no match (server component)
  </acceptance_criteria>
  <done>src/app/terms/page.tsx exists, renders Terms of Service in Italian with all 8 sections, has a "← Torna all'app" back link to "/", TypeScript compiles clean.</done>
</task>

</tasks>

<verification>
After both tasks:
- `npx tsc --noEmit` passes with no errors
- `curl http://localhost:3000/privacy` (dev server running) returns HTML containing "Privacy Policy"
- `curl http://localhost:3000/terms` returns HTML containing "Termini di Servizio"
- Both pages render in browser without JavaScript errors
- Back link returns user to the main app at "/"
</verification>

<success_criteria>
Two public Next.js pages exist at /privacy and /terms, both in Italian, using the existing design system (var(--terra), var(--cream), var(--sepia), .card-warm, .font-display, .bg-texture), each with all required legal sections and a "← Torna all'app" back link. No external dependencies added.
</success_criteria>

<output>
After completion, create `.planning/phases/08-legal-privacy-policy-e-terms-of-service-pagine-pubbliche-consenso-esplicito-al-signup/08-01-SUMMARY.md`
</output>
