---
phase: 09-abbonamenti-e-pagamenti
plan: 00
type: execute
wave: 0
depends_on: []
files_modified:
  - src/lib/__tests__/subscriptions.test.ts
  - src/lib/__tests__/fetchRecipes.test.ts
  - src/app/api/webhooks/stripe/__tests__/route.test.ts
  - src/lib/__tests__/regenerationLimits.test.ts
autonomous: true
requirements: [SUB-01, SUB-02, SUB-03, SUB-04, SUB-05, SUB-06]

must_haves:
  truths:
    - "Test stubs exist for all unit-testable subscription requirements before implementation begins"
    - "Running npm test includes the new test files without errors (tests can be skipped/todo but must load)"
  artifacts:
    - path: "src/lib/__tests__/subscriptions.test.ts"
      provides: "Test stubs for getSubscription behavior (SUB-01, SUB-02)"
      contains: "getSubscription"
    - path: "src/lib/__tests__/fetchRecipes.test.ts"
      provides: "Test stubs for tier-aware fetchRecipes (SUB-03, SUB-04)"
      contains: "fetchRecipes"
    - path: "src/app/api/webhooks/stripe/__tests__/route.test.ts"
      provides: "Test stubs for webhook signature verification (SUB-05)"
      contains: "constructEvent"
    - path: "src/lib/__tests__/regenerationLimits.test.ts"
      provides: "Test stubs for regeneration limit logic (SUB-06)"
      contains: "canRegenerate"
  key_links: []
---

<objective>
Create test stub files for all unit-testable subscription requirements identified in RESEARCH.md Wave 0 Gaps. These stubs define the expected behaviors (RED phase of TDD) so that implementation plans can run tests against them.

Purpose: Nyquist compliance -- every automated `<verify>` command in subsequent plans must have a test file to run against. Without these stubs, verification commands reference non-existent files.
Output: Four test stub files with `describe`/`it` blocks marked as `todo` or with failing assertions.
</objective>

<execution_context>
@C:/Users/ivan9/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/ivan9/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/09-abbonamenti-e-pagamenti-stripe-trial-14gg-piano-base-4-99-mese-piano-pro-7-99-mese-feature-gating/09-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create subscription and fetchRecipes test stubs</name>
  <files>src/lib/__tests__/subscriptions.test.ts, src/lib/__tests__/fetchRecipes.test.ts</files>
  <action>
1. Create `src/lib/__tests__/subscriptions.test.ts` with the following test stubs using Vitest. Use `it.todo()` for tests that will be implemented when the production code exists:

```typescript
import { describe, it, expect, vi } from "vitest";

// Will be importable after Plan 01 creates src/lib/stripe.ts
// import { getSubscription } from "@/lib/stripe";

describe("getSubscription", () => {
  it.todo("returns tier=free when no subscription row exists (SUB-01)");
  it.todo("returns tier=pro and isTrialing=true when status is trialing (SUB-02)");
  it.todo("returns tier=base when status is active and plan_tier is base");
  it.todo("returns tier=pro when status is active and plan_tier is pro");
  it.todo("returns tier=free when subscription status is canceled");
});
```

2. Create `src/lib/__tests__/fetchRecipes.test.ts`:

```typescript
import { describe, it, expect, vi } from "vitest";

// Will be importable after Plan 03 modifies src/lib/supabase.ts
// import { fetchRecipes } from "@/lib/supabase";

describe("fetchRecipes tier-aware", () => {
  it.todo("applies .limit(100) and .neq('added_by', 'ai') for base tier (SUB-03, SUB-04)");
  it.todo("does NOT apply limit for pro tier");
  it.todo("does NOT apply limit for free tier (trial = full access)");
  it.todo("defaults to pro tier when no tier parameter is passed (backward compat)");
});
```

Ensure the `src/lib/__tests__/` directory is created if it does not exist.
  </action>
  <verify>
    <automated>test -f src/lib/__tests__/subscriptions.test.ts && test -f src/lib/__tests__/fetchRecipes.test.ts && grep -q "getSubscription" src/lib/__tests__/subscriptions.test.ts && grep -q "fetchRecipes" src/lib/__tests__/fetchRecipes.test.ts && npm test 2>&1 | tail -5 && echo "PASS"</automated>
  </verify>
  <done>Test stub files for subscriptions and fetchRecipes exist with todo test cases. npm test loads them without errors.</done>
</task>

<task type="auto">
  <name>Task 2: Create webhook route and regeneration limits test stubs</name>
  <files>src/app/api/webhooks/stripe/__tests__/route.test.ts, src/lib/__tests__/regenerationLimits.test.ts</files>
  <action>
1. Create `src/app/api/webhooks/stripe/__tests__/route.test.ts`:

```typescript
import { describe, it, expect, vi } from "vitest";

// Will be importable after Plan 02 creates the webhook route
// import { POST } from "@/app/api/webhooks/stripe/route";

describe("Stripe webhook handler", () => {
  it.todo("rejects request with missing stripe-signature header with 400 (SUB-05)");
  it.todo("rejects request with invalid signature with 400 (SUB-05)");
  it.todo("returns 200 for valid customer.subscription.created event");
  it.todo("upserts subscription row on customer.subscription.updated");
  it.todo("handles customer.subscription.deleted event");
});
```

2. Create `src/lib/__tests__/regenerationLimits.test.ts`:

```typescript
import { describe, it, expect } from "vitest";

// Will be importable after Plan 03 creates src/lib/regenerationLimits.ts
// import { canRegenerate, createRigeneraEntry } from "@/lib/regenerationLimits";

describe("canRegenerate", () => {
  it.todo("returns allowed=true for pro tier (always allowed) (SUB-06)");
  it.todo("returns allowed=true for free tier (always allowed)");
  it.todo("returns allowed=true for base tier with 0 regenerations today");
  it.todo("returns allowed=false with reason=daily for base tier with 2 regenerations today (SUB-06)");
  it.todo("returns allowed=false with reason=weekly for base tier with 3 unique days regenerated");
  it.todo("allows regeneration on an already-regenerated day even at weekly limit");
});

describe("createRigeneraEntry", () => {
  it.todo("creates entry with day name and ISO timestamp");
});
```

Ensure the `src/app/api/webhooks/stripe/__tests__/` directory is created if it does not exist.
  </action>
  <verify>
    <automated>test -f src/app/api/webhooks/stripe/__tests__/route.test.ts && test -f src/lib/__tests__/regenerationLimits.test.ts && grep -q "constructEvent\|stripe-signature\|webhook" src/app/api/webhooks/stripe/__tests__/route.test.ts && grep -q "canRegenerate" src/lib/__tests__/regenerationLimits.test.ts && npm test 2>&1 | tail -5 && echo "PASS"</automated>
  </verify>
  <done>Test stub files for webhook route and regeneration limits exist with todo test cases. npm test loads them without errors.</done>
</task>

</tasks>

<verification>
- `npm test` passes (todo tests are counted but not failures)
- All four test files exist at the expected paths
- Each file contains describe/it blocks covering the requirements from RESEARCH.md Wave 0 Gaps
</verification>

<success_criteria>
- Four test stub files created matching RESEARCH.md Wave 0 Gaps
- npm test runs without errors (todo tests are acceptable)
- Subsequent plans' `<verify>` commands can reference these test files
</success_criteria>

<output>
After completion, create `.planning/phases/09-abbonamenti-e-pagamenti-stripe-trial-14gg-piano-base-4-99-mese-piano-pro-7-99-mese-feature-gating/09-00-SUMMARY.md`
</output>
