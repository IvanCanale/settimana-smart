---
phase: 3
slug: engine-hardening-and-cloud-sync
status: draft
shadcn_initialized: true
preset: "style=default baseColor=neutral cssVariables=true"
created: 2026-03-21
---

# Phase 3 — UI Design Contract

> Visual and interaction contract for Phase 3: Engine Hardening and Cloud Sync.
> Generated by gsd-ui-researcher, verified by gsd-ui-checker.

---

## Design System

| Property | Value |
|----------|-------|
| Tool | shadcn (initialized — components.json present) |
| Preset | style=default, baseColor=neutral, cssVariables=true |
| Component library | radix-ui (via shadcn) |
| Icon library | lucide-react |
| Font | DM Sans (body, labels, UI text) · Playfair Display (display headings only) |

Source: `components.json` + `src/app/globals.css`

---

## Spacing Scale

Declared values (must be multiples of 4):

| Token | Value | Usage |
|-------|-------|-------|
| xs | 4px | Icon gaps, inline padding, tab-nav internal gap |
| sm | 8px | Compact element spacing, shopping-item internal padding |
| md | 16px | Default element spacing, card padding, banner horizontal padding |
| lg | 24px | Section padding, modal content padding |
| xl | 32px | Layout gaps between major sections |
| 2xl | 48px | Major section breaks |
| 3xl | 64px | Page-level spacing |

Exceptions:
- Offline banner: 8px vertical padding (sm token) — compact to avoid stealing vertical space from the plan view
- Sync status text in AppHeader: no additional padding — inline with existing header row at natural line height
- Touch targets for banner dismiss button (if present): minimum 44px × 44px

---

## Typography

| Role | Size | Weight | Line Height |
|------|------|--------|-------------|
| Body | 16px | 400 (regular) | 1.5 |
| Label | 14px | 600 (semibold) | 1.4 |
| Caption / status text | 12px | 400 (regular) | 1.4 |
| Display / heading | 22–32px (clamp) | 600 (semibold), font-display class | 1.1 |

Notes:
- Sync status text in AppHeader uses 12px (Caption) / weight 400 — existing `fontSize: 11` pattern in AppHeader.tsx is superseded by this contract; 12px is the minimum declared size
- Offline banner body text uses 14px / weight 400 — uses the Label size without semibold to signal informational (not urgent) tone
- Shared-ingredient metric line ("X ingredienti condivisi tra i pasti") uses 14px (Label) / weight 600 / color var(--olive) — matches stat-chip uppercase label pattern
- Display headings use weight 600; clamp size range (22–32px) alone provides the visual hierarchy distinction from Label

---

## Color

| Role | Value | Usage |
|------|-------|-------|
| Dominant (60%) | var(--cream) `#FAF5ED` | Page background, meal-slot backgrounds, shopping-item backgrounds |
| Secondary (30%) | var(--warm-white) `#FFFDF8` | Cards (card-warm), shopping-category cards, day-card, modal backgrounds |
| Accent (10%) | var(--terra) `#C4673A` | Active tab indicator, primary CTA buttons, sync-error icon, section-icon backgrounds |
| Offline banner surface | rgba(196,103,58,0.08) with border rgba(196,103,58,0.2) | Offline banner background — uses existing .alert-banner pattern |
| Sync: saving | var(--sepia-light) `#6B4C3B` | "↑ salvataggio..." text — neutral, not alarming |
| Sync: saved | var(--olive) `#5C6B3A` | "✓ salvato" text — positive confirmation |
| Sync: error | var(--terra) `#C4673A` | "⚠ errore sync" text — attention-drawing but not destructive red |
| Shared-ingredient metric | var(--olive) `#5C6B3A` | "X ingredienti condivisi" — green signals benefit, not an alert |
| Destructive | oklch(0.577 0.245 27.325) `≈ #D94F2A` | Destructive actions only (none in this phase) |

Accent (var(--terra)) reserved for:
1. Active tab in tab-nav (.tab-item.active)
2. Primary CTA buttons (.btn-terra)
3. Section icon backgrounds (.section-icon)
4. Sync error state text in AppHeader
5. Offline banner left-accent border (if styled with left border variant)

Source: `src/app/globals.css` custom tokens — pre-existing, not new to this phase.

---

## New UI Elements (Phase 3 Specific)

### 1. Offline Banner

- **Placement**: Below AppHeader, above tab-nav. Full-width within the page container.
- **Visible when**: `navigator.onLine === false` (detected via `online`/`offline` events on `window`)
- **Component class**: `.alert-banner` (already defined in globals.css) — no new CSS needed
- **Content**: Icon `WifiOff` (lucide) + text "Modalità offline — modifiche salvate localmente"
- **Dismissible**: No — it auto-hides when `navigator.onLine` returns true. No manual dismiss.
- **Animation**: `.animate-in` fade-slide-up (already defined)
- **Height**: auto, approximately 40–44px at 14px body text with 8px vertical padding

### 2. Sync Status in AppHeader

Existing rendering already in `AppHeader.tsx` — three states rendered as inline text:
- `saving`: "↑ salvataggio..." in var(--sepia-light), 12px, weight 400
- `saved`: "✓ salvato" in var(--olive), 12px, weight 400
- `error`: "⚠ errore sync" in var(--terra), 12px, weight 400

Phase 3 activates this UI by wiring real sync logic. Visual values updated to contract (12px / weight 400) from existing 11px / weight 500.

### 3. Shared-Ingredient Metric in ShoppingTab

- **Placement**: Immediately above the shopping category grid, below the SectionHeader
- **Visible when**: `generated.stats.reusedIngredients > 0`
- **Component class**: `.stat-chip` with modified layout — single-row inline text variant
- **Content**: `♻️ {N} ingredienti condivisi tra i pasti` where N = `generated.stats.reusedIngredients`
- **Typography**: 14px, weight 600, color var(--olive)
- **Background**: var(--cream) with border rgba(61,43,31,0.12) — matches existing .stat-chip
- **Width**: `fit-content` — does not stretch full width

---

## Copywriting Contract

| Element | Copy |
|---------|------|
| Primary CTA (sync trigger, implicit) | No explicit CTA — sync is automatic and silent (source: CONTEXT.md decision) |
| Offline banner text | "Modalità offline — modifiche salvate localmente" |
| Sync saving | "↑ salvataggio..." |
| Sync saved | "✓ salvato" |
| Sync error | "⚠ errore sync" |
| Shared ingredient metric | "♻ {N} ingredienti condivisi tra i pasti" |
| Shopping empty state (existing, unchanged) | "Nessun ingrediente da acquistare con questi parametri." |
| Allergen gate error (internal only, not shown to user) | Log: "Allergen gate failed for plan seed {N}, attempt {attempt}/3 — retrying with seed {N+1}" |
| Allergen gate hard failure (after 3 retries) | "Non è stato possibile generare un piano sicuro. Riprova modificando le preferenze." — displayed in existing .alert-banner pattern |

Notes:
- No destructive actions in this phase (no delete, no logout, no data removal)
- All copy is in Italian — consistent with existing app language (source: REQUIREMENTS.md, existing components)
- "Modalità offline" uses the informational tone — not alarming. No exclamation mark.

---

## Interaction Contracts

### Auto-Save (CLOUD-01)

- Trigger: `useEffect` watching `generated` — fires when plan changes
- Debounce: 2 seconds after last change (Claude's discretion, per CONTEXT.md)
- User visibility: syncStatus transitions idle → saving → saved (or error)
- No user action required — fully silent
- `saved` state auto-resets to `idle` after 3 seconds

### Offline Mode (CLOUD-02)

- Detection: `window.addEventListener('online' | 'offline')`
- Banner appears: immediate on `offline` event
- Banner disappears: immediate on `online` event + triggers sync
- Plan generation: works offline (engine is local-only)
- No sync during offline — queues to local, pushes on reconnect

### Allergen Gate Retry (ENGINE-01)

- Retry: max 3 attempts with seed+1
- UI during retry: syncStatus stays at `saving` equivalent — no separate loading state
- Hard failure after 3 retries: shows `.alert-banner` with failure message (see Copywriting)
- Success path: invisible to user — plan appears normally

### Protein Variety Constraint (ENGINE-02)

- Entirely engine-internal — zero UI surface
- Testable via planEngine.test.ts

### Shared Ingredient Metric (ENGINE-03)

- Read-only display — no interaction
- Updates reactively when `generated` changes

---

## Component Inventory

Components already existing that Phase 3 uses without modification:
- `AppHeader.tsx` — sync status display already wired (update fontSize from 11px to 12px, weight from 500 to 400)
- `ShoppingTab.tsx` — receives updated stats
- `.alert-banner` CSS class — used for offline banner and allergen hard failure
- `.stat-chip` CSS class — used for shared-ingredient metric
- `FreezeToast.tsx` — not used in Phase 3 (no blocking operations)

New component:
- `OfflineBanner.tsx` — thin wrapper around `.alert-banner` + WifiOff icon + conditional render. Located at `src/components/OfflineBanner.tsx`.

---

## Registry Safety

| Registry | Blocks Used | Safety Gate |
|----------|-------------|-------------|
| shadcn official | badge, button, card, checkbox, input, label, select, slider, tabs, textarea | not required |
| Third-party | none | not applicable |

No third-party registries declared. Safety gate not required.

Source: `components.json` — `"registries": {}` confirms no third-party registries.

---

## Checker Sign-Off

- [ ] Dimension 1 Copywriting: PASS
- [ ] Dimension 2 Visuals: PASS
- [ ] Dimension 3 Color: PASS
- [ ] Dimension 4 Typography: PASS
- [ ] Dimension 5 Spacing: PASS
- [ ] Dimension 6 Registry Safety: PASS

**Approval:** pending
